#####################################################################################
# Set of DEV environment composer files that need to be run before the installation.
# These are run after the files in the composer.sh file.
#
# The composer files in this array will only be installed if the environment = dev.
#####################################################################################

MAKE_FILES=(
  "development/composer.json"
)
