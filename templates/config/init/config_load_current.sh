################################################################################
# When a project is initiated for the first time (bin/init), the config
# variables are collected trough the script and written to the config.sh file.
#
# Add in this script the default values (get from the empty config file).
# Prefix the variable to store the loaded value with INIT_CONFIG.
# Example:
# INIT_CONFIG_CUSTOM_VARIABLE="${CUSTOM_VARIABLE}"
#
# You can set a default value (if no value is found in the config file) by
# adding :-default_value (variable substitution,
# http://tldp.org/LDP/abs/html/parameter-substitution.html) to the assignment:
# Example:
# INIT_CONFIG_CUSTOM_VARIABLE="${CUSTOM_VARIABLE:-noValueYet}"
################################################################################

#INIT_CONFIG_CUSTOM_VARIABLE="${CUSTOM_VARIABLE}"
