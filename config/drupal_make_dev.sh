################################################################################
# Set of DEV environment make files that need to be run before the installation.
# These are run after the files in the make.sh file.
#
# The make files in this array will only be installed if the ennvironment = dev.
################################################################################

MAKE_FILES=(
  "development.make"
)
