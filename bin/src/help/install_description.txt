The install script will install a new website in the web directory by
downloading Drupal core, contrib modules & themes using the make files in the
configuration directory.

  If there is a working installation:
  * A backup of the web directory and the database will be created.
  * The web directory will be deleted and all files will be destroyed.
  * The database will be overwritten.
