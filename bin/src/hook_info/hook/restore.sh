markup_h2 "Restore platform from a backup"
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}restore_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}restore_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
