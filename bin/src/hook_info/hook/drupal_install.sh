markup_h2 "Install Drupal"
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}drupal_install_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}drupal_install_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
