markup_h2 "Check & Install/Update the project file structure"
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}init_structure_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}init_structure_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
