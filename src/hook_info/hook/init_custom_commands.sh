markup_h2 "Symlink custom commands"
markup "Will only be called if there are custom commands to symlink."
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}init_custom_commands_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}init_custom_commands_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
