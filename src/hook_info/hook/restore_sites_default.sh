markup_h2 "Move the sites/default directory back to the web directory"
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}restore_sites_default_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}restore_sites_default_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
