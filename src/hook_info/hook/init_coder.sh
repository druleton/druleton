markup_h2 "Install or update drupal coder"
markup "Will only be called if the installation of coder is not disabled."
markup "${GREY}config/${SCRIPT_NAME}/${RESTORE}init_coder_before${GREY}(_${ENVIRONMENT})${RESTORE}.sh
${GREY}config/${SCRIPT_NAME}/${RESTORE}init_coder_after${GREY}(_${ENVIRONMENT})${RESTORE}.sh
"
